{
  "name": "@relaychat/client-sdk",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"client-sdk package has no eslint config yet\""
  },
  "dependencies": {
    "@relaychat/protocol": "workspace:*"
  }
}
